import { synHi } from 'core/utils'
import jsText from './js.sample'

export default `
<section>
  <h2>Create context and nodes</h2>
  <pre class="example-code language-js"><code>${synHi(jsText)}</code></pre>
</section>
`
